import React from 'react'

const Info = ({ text }) => {
    return (
        <div className="max-w-3xl p-5 m-auto font-semibold text-center">
            {text}
        </div>
    )
}

export default Info
